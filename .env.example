# CXXtract2 environment variables example
# Copy to .env and fill secrets for local development.

# Core app config (optional)
# CXXTRACT_DB_PATH=./cxxtract_cache.db
# CXXTRACT_HOST=127.0.0.1
# CXXTRACT_PORT=8000

# GitLab tokens referenced by workspace.yaml token_env_var fields.
# Use env var names in workspace.yaml, not raw token strings.
CXXTRACT_GITLAB_TOKEN_PROJECT_CLOUD=replace_with_real_token
CXXTRACT_GITLAB_TOKEN_WEBRTC=replace_with_real_token

# Optional: override feature toggles
# CXXTRACT_ENABLE_VECTOR_FEATURES=true
